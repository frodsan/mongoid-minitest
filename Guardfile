guard 'minitest' do
  watch(%r|^test/(.*)_test\.rb|)
  watch(%r|^test/test_helper\.rb|) { "test" }
  watch(%r|^test/models/(.*)\.rb|) { "test" }
  watch(%r|^lib/mongoid-minitest/matchers/(.*)\.rb|) { "test" }
end
